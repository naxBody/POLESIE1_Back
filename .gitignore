Nothing should be ignored in this case, as the only added file is a SQL script (`polesie/sql/database_update.sql`) which is a source/config file and not a build artifact, dependency, or temporary file.