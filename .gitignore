Nothing should be ignored in this case, as the only modified file is a SQL source file (`polesie/sql/database_updated.sql`) which is clearly a source code file that should be tracked in version control.

(empty)